function [] = load_transfr_fn(obj)
    %
    % Read SCM transfer function data and frequencies from a file and store them
    % in the object properties 'transfr_fn' and 'transfr_freq'.
    %
    % Transfer functions are instrument-specific. This is just a placeholder
    % method and should be over-ridden by your specific @data_merge object.
    %
    
    error(['This is just a place-holder function. ' ...
           'Overwrite this method to make it data-type/mission specific.'])
end